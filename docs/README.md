# 🎾TENNIS MATCH SCOREBOARD

**Tennis Match Scoreboard** — это веб-приложение для ведения счёта теннисных матчей в реальном времени с сохранением 
результатов в базу данных. Проект реализован на классическом Java-стеке без использования тяжеловесных
фреймворков (Spring) с целью глубокого понимания архитектурных паттернов, спецификации Jakarta Servlet и
работы ORM Hibernate.

---

## 🚀 Функционал приложения

1.  **Создание матча (`/new-match`):** Форма ввода уникальных имён двух игроков. Автоматически проверяет наличие
игроков в БД (и создает новых при необходимости), генерирует уникальный `UUID` матча и инициализирует счёт.


2.  **Интерактивное табло (`/match-score?uuid=`):** Реализована система подсчёта очков в теннисе 
(0, 15, 30, 40, ровно/больше-меньше).
   * Матчи играются в формате *Best of 3* (до 2 выигранных сетов).
   * При счёте 6/6 по геймам в сете разыгрывается *тай-брейк* до 7 очков.

3.  **Архив матчей (`/matches`):** Постраничный просмотр (пагинация) всех завершенных игр с возможностью 
фильтрации и поиска по паттерну имени и фильтрации по количеству отображаемых матчей.

---

## 🛠 Технологический стек

![Java](https://img.shields.io/badge/Java%2021-ED8B00?style=for-the-badge&logo=openjdk&logoColor=white)
![Maven](https://img.shields.io/badge/Maven-C71A36?style=for-the-badge&logo=apachemaven&logoColor=white)
![Tomcat](https://img.shields.io/badge/Tomcat%2011-F8DC75?style=for-the-badge&logo=apachetomcat&logoColor=black)
![Hibernate](https://img.shields.io/badge/Hibernate%206.5-59666C?style=for-the-badge&logo=hibernate&logoColor=white)
![Redis](https://img.shields.io/badge/Redis-DD0031?style=for-the-badge&logo=redis&logoColor=white)
![JUnit5](https://img.shields.io/badge/JUnit%205-25A162?style=for-the-badge&logo=junit5&logoColor=white)

* **Backend:** Java 21, Jakarta Servlet API 6.1, JSP / JSTL
* **Data & Persistence:** Hibernate ORM 6.5 + интеграция HikariCP, H2 Database (In-Memory), Redis (Jedis 7.4) 
для хранения текущих матчей
* **Утилиты:** Lombok (кодогенерация), MapStruct (маппинг DTO), Jackson Databind (сериализация для Redis), 
Logback + SLF4J (логирование)
* **Frontend:** HTML/CSS вёрстка

---

## 🏗 Архитектура и структура проекта

Приложение спроектировано по принципам многослойной архитектуры с выраженным разделением ответственности
и использованием паттерна **MVCS (Model-View-Controller-Service)**, а также элементов **DDD (Domain-Driven Design)** 
для изоляции правил теннисного счёта.

### 📂 Общая структура пакетов

```text
.
├── docs/
└── src/
    ├── main/
    │   ├── java/
    │   │   └── com/
    │   │       └── project/
    │   │           ├── controller/         # Слой представления (Сервлеты, обработка HTTP-запросов)
    │   │           ├── dao/                # Слой доступа к БД H2 (Data Access Object)
    │   │           ├── domain/             # Чистая бизнес-логика (Rich Domain Model правила тенниса)
    │   │           │   └── game/
    │   │           ├── dto/                # Объекты переноса данных (Data Transfer Objects)
    │   │           │   ├── match/
    │   │           │   ├── request/
    │   │           │   └── response/
    │   │           ├── entity/             # ORM-сущности для таблицы SQL базы данных
    │   │           ├── exception/          # Иерархия кастомных исключений
    │   │           ├── factory/            # Настройка и экспорт потокобезопасных клиентов (Hibernate/Redis)
    │   │           ├── filter/             # Сервлетные фильтры
    │   │           ├── listener/           # Жизненный цикл контейнера и ручное внедрение зависимостей
    │   │           ├── mapper/             # Мапперы MapStruct
    │   │           ├── storage/            # Слой оперативного Key-Value хранилища (Redis) для живых матчей
    │   │           └── util/               # Общие вспомогательные утилитарные классы
    │   │
    │   ├── resources/                      # Конфигурационные файлы проекта (СУБД, Redis, логирование)
    │   └── webapp/                         # Веб-интерфейс приложения (Статика и JSP)
    │       ├── css/                        # Стили интерфейса
    │       └── WEB-INF/                    # Защищенная системная директория веб-контейнера
    │           └── view/                   # Изолированные JSP-шаблоны страниц
    │               └── fragment/           # Переиспользуемые UI-компоненты страниц (шапка, подвал)
    │
    └── test/                               # Модульное тестирование (JUnit 5)
        └── java/
            └── com/
                └── project/
                    └── domain/             # Тест-кейсы для проверки теннисных правил матча и сета
                        └── game/
```

---

## 📊 Структура Базы Данных

Поскольку используется база данных *In-Memory*, схемы таблиц и заполнение данными инициализируются автоматически
при каждом запуске приложения из JPA-моделей и SQL-скрипта.

### Таблица `Players` (Игроки)

| Столбец  | Тип данных  | Ограничения        | Описание                        |
|:---------|:------------|:-------------------|:--------------------------------|
| **ID**   | INT         | PK, Auto-Increment | Уникальный идентификатор игрока |
| **NAME** | VARCHAR(40) | UNIQUE, NOT NULL   | Имя или никнейм теннисиста      |

### Таблица `Matches` (Завершенные матчи)

| Столбец           | Тип данных | Ограничения                    | Описание                       |
|:------------------|:-----------|:-------------------------------|:-------------------------------|
| **ID**            | INT        | PK, Auto-Increment             | Уникальный идентификатор матча |
| **FIRST_PLAYER**  | INT        | FK ➡️ `Players(ID)`, NOT NULL  | Ссылка на первого игрока       |
| **SECOND_PLAYER** | INT        | FK ➡️ `Players(ID)`, NOT NULL  | Ссылка на второго игрока       |
| **WINNER**        | INT        | FK ➡️ `Players(ID)`, NOT NULL  | Ссылка на победителя матча     |

> 🛡️ **Валидация на уровне СУБД (CHECK):** > В базу невозможно записать некорректный матч благодаря ограничению
`FIRST_PLAYER <> SECOND_PLAYER AND (WINNER = FIRST_PLAYER OR WINNER = SECOND_PLAYER)`. Оно гарантирует, 
что соперники уникальны, а выиграть может только участник матча.
---

